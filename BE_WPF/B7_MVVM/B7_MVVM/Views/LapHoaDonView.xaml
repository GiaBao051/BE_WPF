<UserControl x:Class="B7_MVVM.Views.LapHoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000"
             Background="White">
    <Grid Margin="10" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ QUÁN CAFE SINH VIÊN"
                   Foreground="DarkOrange"
                   FontWeight="Bold"
                   FontSize="24"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Tên khách hàng" VerticalAlignment="Center" Margin="0,0,10,6"/>
                    <TextBox Grid.Column="1" Text="{Binding TenKhachHang, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,6"/>

                    <TextBlock Grid.Row="1" Text="Số điện thoại" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SoDienThoai, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                    <CheckBox Grid.Row="1" Grid.Column="2" Content="Sinh viên?" IsChecked="{Binding LaSinhVien}" VerticalAlignment="Center"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Header="Chọn bàn" Margin="0,0,10,0">
                        <StackPanel Margin="5">
                            <RadioButton Content="Bàn 01" GroupName="Ban" IsChecked="{Binding Ban01}" Margin="0,0,0,5"/>
                            <RadioButton Content="Bàn 02" GroupName="Ban" IsChecked="{Binding Ban02}" Margin="0,0,0,5"/>
                            <RadioButton Content="Bàn 03" GroupName="Ban" IsChecked="{Binding Ban03}" Margin="0,0,0,5"/>
                            <RadioButton Content="Bàn 04" GroupName="Ban" IsChecked="{Binding Ban04}"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Grid.Column="1" Header="Thức ăn" Margin="0,0,10,0">
                        <ItemsControl ItemsSource="{Binding DanhSachThucAn}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding TenMon}" IsChecked="{Binding IsSelected}" Margin="5,3"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>

                    <GroupBox Grid.Column="2" Header="Nước uống">
                        <ItemsControl ItemsSource="{Binding DanhSachNuocUong}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding TenMon}" IsChecked="{Binding IsSelected}" Margin="5,3"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button Content="Chọn" Width="90" Margin="0,0,10,0" Command="{Binding ChonCommand}"/>
                    <Button Content="Nhập lại" Width="90" Command="{Binding NhapLaiCommand}"/>
                </StackPanel>

                <GroupBox Grid.Row="3" Header="Hóa đơn (sau khi nhấn Chọn)">
                    <ListView ItemsSource="{Binding DanhSachHoaDonTam}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Khách hàng" DisplayMemberBinding="{Binding KhachHang.TenKhachHang}" Width="130"/>
                                <GridViewColumn Header="SĐT" DisplayMemberBinding="{Binding KhachHang.SoDienThoai}" Width="100"/>
                                <GridViewColumn Header="Bàn" DisplayMemberBinding="{Binding ViTriBan}" Width="70"/>
                                <GridViewColumn Header="Nước uống" DisplayMemberBinding="{Binding NuocUongHienThi}" Width="150"/>
                                <GridViewColumn Header="Thức ăn" DisplayMemberBinding="{Binding ThucAnHienThi}" Width="150"/>
                                <GridViewColumn Header="Tổng tiền" DisplayMemberBinding="{Binding TongTienThanhToan, StringFormat={}{0:N0}}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </GroupBox>
            </Grid>

            <GroupBox Grid.Column="1" Header="Thông tin khách đã chọn">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="Khách hàng:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding HoaDonTam.KhachHang.TenKhachHang}" Margin="0,0,0,8"/>

                        <TextBlock Text="Sinh viên:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding HoaDonTam.SinhVienHienThi}" Margin="0,0,0,8"/>

                        <TextBlock Text="Vị trí bàn:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding HoaDonTam.ViTriBan}" Margin="0,0,0,8"/>

                        <TextBlock Text="Nước uống:" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding HoaDonTam.DanhSachNuocUong}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TenMon, StringFormat=- {0}}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Thức ăn:" FontWeight="Bold" Margin="0,8,0,0"/>
                        <ItemsControl ItemsSource="{Binding HoaDonTam.DanhSachThucAn}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding TenMon, StringFormat=- {0}}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Tổng tiền:" FontWeight="Bold" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding HoaDonTam.TongTienThanhToan, StringFormat={}{0:N0} ?}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="Thanh toán" Width="90" Margin="0,0,10,0" Command="{Binding ThanhToanCommand}"/>
                        <Button Content="Thoát" Width="90" Command="{Binding ThoatCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
