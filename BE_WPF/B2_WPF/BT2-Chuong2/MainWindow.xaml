<Window x:Class="BT2_Chuong2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BT2_Chuong2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="{DynamicResource WindowBackground}">
        <!-- StackPanel chứa label + combobox ở góc trên bên trái -->
        <StackPanel Orientation="Horizontal"
            VerticalAlignment="Top"
            HorizontalAlignment="Left"
            Margin="10,10,0,0">
            <!-- 10 pixel cách 2 cạnh trên và trái -->
            <TextBlock Text="Chọn Theme: " 
               Foreground="{DynamicResource WindowForeground}"
               VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="cbTheme" Width="80" SelectionChanged="cbTheme_SelectionChanged">
                <ComboBoxItem Content="Sáng" IsSelected="True"/>
                <ComboBoxItem Content="Tối"/>
            </ComboBox>
        </StackPanel>

        <!-- StackPanel chứa Button ở giữa màn hình -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Nút mẫu" Width="120" Height="50"
            Background="{DynamicResource PrimaryButtonColor}"
            Foreground="{DynamicResource WindowForeground}"/>
        </StackPanel>
    </Grid>
</Window>
